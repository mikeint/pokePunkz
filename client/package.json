{
  "name": "profile",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "aos": "^2.3.4",
    "node-sass": "^6.0.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-gtm-module": "^2.0.11",
    "react-router-dom": "^5.2.0",
    "react-scripts": "4.0.3",
    "react-vertical-timeline-component": "^3.3.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "node updateJSON.js && aws s3 sync build/ s3://pokepunkz.io --acl public-read && aws s3 sync metadata s3://pokepunkz.io/metadata --acl public-read --content-type application/json",
    "cc": "aws cloudfront create-invalidation --distribution-id E26KPMQGQFJ783 --paths '/*'",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
